{
  "name": "src",
  "version": "1.0.0",
  "description": "bazaaar.io",
  "author": "BlockBase",
  "private": true,
  "scripts": {
    "dev": "NODE_ENV=sand nuxt --hostname 0.0.0.0 --port 3000",
    "dev:stg": "NODE_ENV=staging nuxt --hostname 0.0.0.0 --port 3000",
    "dev:prod": "NODE_ENV=production nuxt --hostname 0.0.0.0 --port 3000",
    "dev:maintenance": "MAINTENANCE_MODE=true nuxt --hostname 0.0.0.0 --port 3000",
    "build:dev": "NODE_ENV=sand nuxt build",
    "build:stg": "NODE_ENV=staging nuxt build",
    "build:prod": "NODE_ENV=production nuxt build",
    "build:maintenance": "MAINTENANCE_MODE=true nuxt build",
    "start": "nuxt start --hostname 0.0.0.0 --port 8080"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^5.7.2",
    "@nuxtjs/axios": "^5.3.6",
    "@nuxtjs/pwa": "^2.6.0",
    "@nuxtjs/vuetify": "^0.5.3",
    "cross-env": "^5.2.0",
    "firebase": "^5.8.4",
    "node-sass": "^4.11.0",
    "nuxt": "^2.3.4",
    "nuxt-maintenance-mode": "^0.3.0",
    "sass-loader": "^7.1.0",
    "vue-i18n": "^8.8.1",
    "web3": "^1.0.0-beta.46",
    "websocket": "^1.0.28"
  },
  "devDependencies": {
    "nodemon": "^1.18.9",
    "prettier": "^1.16.4"
  },
  "engines": {
    "node": "8.x.x"
  }
}
