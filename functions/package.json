{
  "name": "functions",
  "description": "Cloud Functions for Firebase",
  "engines": {
    "node": "8"
  },
  "scripts": {
    "serve:dev": "firebase use dev && firebase serve --only functions",
    "serve:stg": "firebase use stg && firebase serve --only functions",
    "serve:prd": "firebase use prd && firebase serve --only functions",
    "deploy:dev": "firebase use dev && firebase deploy --only functions",
    "deploy:stg": "firebase use stg && firebase deploy --only functions",
    "deploy:prd": "firebase use prd && firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "canvas": "^2.3.1",
    "firebase-admin": "^8.0.0",
    "firebase-functions": "^2.1.0",
    "fs": "0.0.1-security",
    "google-auth-library": "^3.1.1",
    "googleapis": "^38.0.0",
    "twitter": "^1.7.1",
    "typedarray-to-buffer": "^3.1.5",
    "web3": "1.0.0-beta.46",
    "yaeti": "^1.0.2"
  },
  "private": true,
  "devDependencies": {
    "firebase-functions-test": "^0.1.6",
    "firebase-tools": "^6.8.0",
    "prettier": "^1.17.0"
  }
}
